#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (int)(n); i++)
#define REP(i, n) for(int i = 0; i < (int)(n); i++)
const long long INF = 1LL << 60;
int main() 
{
        int n,m,s,ptr=0,l,d,u,r;
    cin >> n >> m >> s;

    int A[s+10][s+10];

    rep(i,n){
        rep(j,n){
            cin >> A[i][j];
        } 
    }

    
}